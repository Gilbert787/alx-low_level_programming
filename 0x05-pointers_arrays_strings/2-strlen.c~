#include "main.h"
#include <string.h>

/**
* _strlen - returns the string length
*
* @s: string initialized
*
* Return: return the string length
*/

int _strlen(char *s)
{
	int len;
	int temp;

	temp = *s;

	len = strlen(*s);

	return (len);
}

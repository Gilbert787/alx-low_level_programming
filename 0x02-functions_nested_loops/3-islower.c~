#include "main.h"

/**
 * _islower - show if a char is lower or upper case
 *
 * @c: int
 *
 * Return: @c value
 */

int _islower(int c);
{
	if (c > 96 && c < 123)
		return (1);

	else
		return (0);

}

#include "main.h"

/**
 * Print_alphabet - prints alphabets from a to z
 *
 * Return: Always 0 (Success)
 */

void print_alphabet(void)
{
	char b = 'a';

	while (b <= 'z')
	{
		_putchar (b);
		b++;
	}

	_putchar ('\n');

}
